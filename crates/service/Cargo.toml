[package]
name = "service"
version = "0.1.0"
edition = "2021"

[dependencies]
abi = { path = "../abi" }
db = { path = "../db" }

sha2 = { version = "0.10" }
serde = { workspace = true, features = ["derive"] }
jwt = { version = "0.16" }
hmac = { version = "0.12" }